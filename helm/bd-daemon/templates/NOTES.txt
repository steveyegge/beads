bd-daemon has been deployed.

RPC endpoint:
  kubectl port-forward svc/{{ include "bd-daemon.daemon.fullname" . }} {{ .Values.daemon.service.port }}:{{ .Values.daemon.service.port }}

{{- if .Values.daemon.http.enabled }}
HTTP endpoint:
  kubectl port-forward svc/{{ include "bd-daemon.daemon.fullname" . }} {{ .Values.daemon.service.httpPort }}:{{ .Values.daemon.service.httpPort }}
{{- end }}

{{- if .Values.dolt.statefulSet.enabled }}
Dolt database: ENABLED (StatefulSet)
  kubectl port-forward svc/{{ include "bd-daemon.dolt.fullname" . }} {{ .Values.dolt.service.port }}:{{ .Values.dolt.service.port }}
{{- end }}

{{- if .Values.redis.enabled }}
Redis WispStore: ENABLED (namespace={{ .Values.redis.namespace }}, TTL={{ .Values.redis.wispTTL }})
{{- else }}
Redis WispStore: DISABLED (using in-memory store)
{{- end }}

{{- if .Values.slackBot.enabled }}
Slack bot: ENABLED (sidecar)
{{- end }}

{{- if .Values.ingress.enabled }}
Ingress: ENABLED (host={{ .Values.ingress.host }})
{{- end }}
